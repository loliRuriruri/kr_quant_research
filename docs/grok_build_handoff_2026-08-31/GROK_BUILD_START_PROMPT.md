# Grok Build 시작 지시문

아래 내용을 Grok Build에 그대로 전달한다.

---

`C:\Users\a4jud\kr_quant_research` 저장소를 직접 열고 작업해 주세요.

가장 먼저 아래 문서를 순서대로 끝까지 읽으세요.

1. `docs/grok_build_handoff_2026-08-31/README.md`
2. `docs/grok_build_handoff_2026-08-31/GROK_BUILD_REMAINING_WORK.md`
3. 루트 `README.md`
4. `ARCHITECTURE.md`

그 다음 `git status --short`, `git diff`, `git log -10 --oneline --decorate`, 최신 태그를 확인하고 현재 상태를 보고하세요. 기존 사용자 변경을 지우거나 되돌리지 마세요. 특히 추적되지 않은 `.agents/`와 루트 `output/`은 사용자 소유이므로 수정·추가·삭제·커밋하지 마세요.

현재 기준 HEAD는 `cd4b197`이고 원격 `origin/main`과 일치해야 합니다. 안전한 롤백 기준은 태그 `smart-pipeline-20260831`입니다. 기준이 다르면 임의로 reset하지 말고 차이를 먼저 보고하세요.

먼저 `GROK_BUILD_REMAINING_WORK.md`의 **P0-1 식별자 계약**과 **P0-2 스마트 실행의 단계별 상태·재시도 계약**만 코드와 실데이터로 재검증하세요. 문서 내용을 무조건 사실로 간주하지 말고, 각 주장에 해당하는 코드·테스트·API 응답을 확인하세요. 그 뒤 첫 수정 범위, 수정 파일, 데이터 마이그레이션 영향, 테스트 계획, 롤백 방법을 보고하고 단계적으로 구현하세요.

작업 원칙:

- 한 단계당 독립 커밋 하나를 기본으로 합니다.
- 각 단계 시작 전 annotated savepoint tag를 만들고 자세한 메시지를 남깁니다.
- targeted test → 전체 pytest → 로컬 서버 재시작 → 실제 API/UI 확인 → commit → push 순서를 지킵니다.
- `git reset --hard`, 사용자 파일 삭제, 비밀정보 출력, 품질 가드 우회 배포를 하지 않습니다.
- KRX 가격과 OpenDART 재무가 정본이며, KIS·Toss·AI·기술지표는 설명/수급 오버레이입니다.
- LLM은 `quant_score`, 팩터 점수, 순위를 수정하면 안 됩니다.
- KIS `FUND`는 “기금”으로 유지하며 국민연금으로 바꾸지 않습니다.
- 주문·잔고·체결·자동매매 기능을 추가하지 않습니다.
- 공개 배포가 차단되면 안전장치를 끄지 말고 시세 기준일·품질·근거 계약을 정상화합니다.

첫 보고에는 다음을 반드시 포함하세요.

1. 현재 실행 중인 작업 유무와 마지막 정상 완료 시각
2. KRX 기대 기준일/실제 기준일
3. DART 사용 가능 재무 종목 수/대상 수/진행 커서
4. 현재 퀀트 적격 종목 수
5. 공개판 마지막 배포 기준일과 현재 로컬판의 동기화 여부
6. P0-1/P0-2에서 실제로 재현된 결함과 재현되지 않은 항목
7. 첫 커밋의 정확한 범위와 완료 조건

---

