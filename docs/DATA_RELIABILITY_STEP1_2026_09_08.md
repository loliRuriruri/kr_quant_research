# 데이터 신뢰성 1단계 — 수집 복구와 판정 보강

## 결과
- 2026-09-08 08:10 KST 공식 KRX 재확인: 9/4 및 9/7 양 시장 응답 모두 정상. 9/7 KOSPI943/KOSDAQ1822행. 이전 빈 응답은 현재 해소됐으나 제공 지연의 상위 원인을 단정하지 않음.
- 기존 서버 작업 관리 API로 krx-prices(9/7, 최근3거래일) 실행 성공. 08:14 재검사에서 저장 시세9/7, 2508관측일, 종목×거래일 키 중복0. 종목정보·거래상태도 함께 갱신.
- 점수와 전략 캐시는9/4: 시세만 최신이며 전체 정상화는 미완료. screen/live 작업은 자동 공개 배포와 연결돼 이번 진단에서 실행하지 않음. 수집 작업 krx-prices는 자동 배포 대상이 아님을 확인.

## 재무와 수급의 실제 범위
- DART 대상2543종목 중2540종목 보유(99.9%), 676764행. 최대 가용일9/2. 이 수치는 최신 공시 완비율 아님.
- 전체 재무 보유2541종목 중2517종목의 마지막 기간말6/30. 대상2543 분모와 전체 보유2541은 다른 집합. 비12월 결산·정정·누락 여부 확인 전 나머지를 오류로 확정하지 않음.
- KIS 저장96종목 중9/7인 종목33, 과거 날짜63. 최신 날짜 하나로 전체 최신 판정 금지. 수집 대상이 매일 달라지는 영향 가능. 전 상장종목 커버리지 아님.

## 패치
- freshness: 오래된 저장 시세와 점수가 같은 날짜라도 최신 점수로 반환하지 않음. 저장 시세 일치 여부는 별도 필드.
- 점수 기준일 누락/불일치를 지연으로 처리.
- 공식 수급 종목별 max 거래일 집계 추가. read_only 연결, DB 미존재시 생성/토큰 발급 없음. DB 접근 실패는 unavailable.
- 재무 보유율의 의미 및 최신 공시 검증 미완료 필드 추가. PC 파이프라인 문구도 재무 행 보유/최신 공시 완비율 아님으로 교정. JS2.81.1.
- 수급 진단은 보조 상태이며 Quant 배점·선정식에 반영하지 않음.

## 검증/근거
- test_freshness, test_smart_sync, test_krx_response_contract 합계43 pass.
- scripts/audit_source_freshness.py: 읽기 전용 집계, 선택적 공식 KRX 프로브, 결과 파일 중복 덮어쓰기 금지.
- output/audit/source-freshness-20260908.json 실제 갱신 후 기록. 동반 audit.ipynb는 같은 함수를 호출하며 별도 Jupyter 커널 실행은 하지 않음.
- 분석 보고서 스킬의 HTML 포장 시도는 `manifest.blocks must include at least one chart block` 제한으로 실패. 단순 기준일·보유율 진단에 불필요한 차트를 추가하지 않았으며 HTML 보고서 완료를 주장하지 않음. artifact.json과 이 기술 작업 기록으로 근거 보존.

## 잔여 / 적용
- 백엔드 패치는 아직 실행 중 서버에 적용되지 않음: 재시작 필요. 이번에 서버를 재시작하지 않았으며 기존 자동 스케줄은 유지. 정적 문구는 화면 새로고침 적용.
- 다음 우선순위: 점수·전략을 최신 시세와 재계산 연결(공개 배포와 분리), DART 최근 공시·정정 대조/갱신 정책, 수급 대상별 신선도 판정 소비 UI/재시도. 1단계 전체 완료 아님.
- 사용자 portfolio/감사 PNG 변경 보존. GitHub push·공개 배포 없음.
- savepoint-before-data-reliability1-20260908 → d3c14db. 코드 롤백은 후속 패치 커밋 revert 권장. 태그는 수집 데이터 백업이 아니며 갱신한9/7 원천을 되돌리지 않음.
