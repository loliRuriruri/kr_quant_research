# AI 도입 3단계 검증 — 2026-09-08

## Grok AUTH 승인 후 실모델 검증

사용자가 유료/Grok AUTH 검증을 명시 승인했다. 평가 도구에 `--provider xai --allow-paid`를 추가했으며 앱의 자동 Tier1 무료 전용 정책은 바꾸지 않았다. 한도·실제 청구액은 확인하지 않았다.

첫 xAI 호출은0.29초 실패, 별도 모델 목록 요청은 HTTP400 Incorrect API key 응답이었다. Grok CLI `models` 첫 실행에서 인증 복구가 이루어진 뒤 재확인 시 grok.com 로그인 상태가 확인됐다. 갱신 후 기존 API 경로가 성공했으므로 AUTH 자체를 API에 사용할 수 없다고 단정했던 초기 추정은 정정한다. 초기 토큰 거부는 확인했지만 만료·폐기 등 세부 원인은 확정하지 않았다.

인증 갱신 직후20초 요청은 시간 초과했다. 명시적 Grok 평가에 한해 HTTP75초/전체90초를 허용했다. 무료 평가는 기존20초/전체60초 유지.

### v4 출력 직접 대조: 수치·근거 5/5 확인, 표현 개선 필요

증거: `audits/ai-stage3/xai-evaluation-20260908T091256Z.json` (가상기업 합성 사례, 실제 투자 검증 아님).

| 사례 | 원본 대조 | 판정 | 초 |
|---|---|---|---:|
| 정상 | 중앙값0.08, 양수6/8, 현재3개월+4% 유지 | 수치 일치, 올해 예측과 구분 | 53.15 |
| 약세 | 중앙값-0.12, 양수2/8, 현재-18% 유지 | 음수 부호 및 반대 근거 유지 | 44.77 |
| 소표본 | 표본2, 양수2/2, 중앙값0.30 | 100%를 미래 성공률로 단정하지 않음 | 54.71 |
| 누락 | 표본5, 양수3, 중앙값0.06 | 수급·뉴스·현재 가격을 만들지 않음 | 53.82 |
| 오래된 자료 | 기준2026-08-01, 최신 시세 누락 | 기준일 이후 최신 시세 확인 제시 | 44.50 |

5건 평균50.19초. 성능 벤치마크나 한도 보장은 아니다. 공통적으로 비용 미포함·과거 관찰과 올해 확인을 구분했고 제공되지 않은 이벤트를 만들지 않았다.
반면 소수 수익률/내부 코드가 노출됐고, 오래된 자료 사례에서는 signal_id의 `stale`를 상태처럼 서술했다. 이는 독자가 오해할 표현 문제로 판단했다.

v5 프롬프트: 비율을 백분율로 표기(이미 %인 근거는 재변환 금지), 중앙값 등 쉬운 말 사용, 식별자를 근거로 해석 금지, 목록 유무 대신 종목 결론 우선. 정상·오래된 자료를 표적 재검증한다.

### v5 표적 재검증 결과: 조건부 사용 가능

증거: `audits/ai-stage3/xai-evaluation-20260908T091551Z.json`.
정상65.13초: 과거+8%, 불리한 움직임-5%, 현재3개월+4%, 기준2026-09-08을 정확히 서술했다. 오래된 자료57.95초: +6%, 3/5건, -5%, 2026-08-01을 보존하고 이후 자료 확인을 제시했다. 식별자/내부 상태 코드 노출은 두 출력에서 사라졌다.
잔여 경미한 표현 문제: 오래된 자료에서 구간을 '창구'라 표현, 정상에서는 빈 가설 배열 대신 '입력된 캘린더 가설 없음' 문자열을 반환했다. 허위 이벤트는 아니지만 문장 품질은 추가 관찰이 필요하다. 정상 출력은 승률이 직접 제공되지 않았다고 했으나6/8을 본문에 그대로 표시했다.

Grok v4 합성5개 사실 대조 + v5 표적2개 재검증 완료. v5 전체5개 재검증, 실제 운영 종목 출력, 다른 모델, 브라우저 시각 검증 및 공개 배포는 미실시다. 이 결과는 일반적 환각 방지나 투자 성과 보장이 아니다. 네모트론 검증 보류를 Grok 성공으로 대체하지 않는다.
회귀 Python24 + JS4 통과. 사용자 포트폴리오 변경은 보존했다. 운영 자동 유료 전환은 변경하지 않았다.

## 세 번째 시도 및 대기 UX 보완

09:01:29 UTC 시작한 평가가 09:03:19 UTC에도 첫 사례에서 대기했다. 110초 이상 종료되지 않아 해당 평가 프로세스만 수동 중단했다. 성공 응답과 완료 JSON은 없으며 실패의 구체 공급자 원인은 미확인이다.
평가 CLI에 60초 별도 대기 상한과 안전한 오류 분류를 추가했다. 이는 운영 API의 강제 취소 기능이 아니다. 시간 초과 시 평가를 종료하며 상위 공급자의 원격 작업 취소까지 보장하지 않는다.
시즌 UI는 같은 카드 요청 중복을 막고, 초기 빈 카드에서8초 후 지연 안내를 표시한다. 이전 분석이 있으면 그대로 유지한다. 통계 영역을 대체하거나 유료로 전환하지 않는다.
실모델 추가 호출은 중단했다. 공급자 정상화 또는 승인된 다른 무료 모델을 확보한 뒤 품질 검증을 재개해야 한다.

## 후속 재검증

운영 프롬프트 재사용 평가 도구 `scripts/evaluate_season_ai.py`와 정상/약세/소표본/누락/지연 합성 사례를 추가했다.
2026-09-08 08:53:50 UTC 기록: 정상 사례가 52.6초 후 RuntimeError로 실패하여 나머지4건은 미실행.
이번 오류 본문은 저장하지 않아 구체 원인은 확정하지 않는다. 성공 출력·사실 검증 통과는 없다.
증거: `audits/ai-stage3/free-evaluation-20260908T085350Z.json`.
실행: 저장소에서 `.venv/Scripts/python.exe scripts/evaluate_season_ai.py`.
최대5회, 첫 실패 중단, 무료 경로만 허용. HTTP timeout20초는 전체 벽시계 시간 상한이 아니다.
실제 기업 평가가 아닌 가상기업 합성 입력이며 운영 데이터/캐시는 바꾸지 않는다.

## 결론

표시·계약 방어는 반영했다. 실제 무료 모델의 분석 품질 승인은 보류한다.
현재 시즌 브리핑을 직접 호출했으나 Nvidia 상위 공급자 과부하(HTTP 502)로 실패했다.
단회 전체 소요 43.41초(원본 준비 + API 호출 포함). 모델만의 지연 또는 평균 지연이 아니다.
응답은 UNAVAILABLE / ai_generated=false / cache.stored=false였고 유료 전환은 없었다.
모델이 생성한 성공 출력이 없어 수치 정확성·인과관계·표현 품질을 통과했다고 주장할 수 없다.

## 적용

- 공통 메뉴 브리핑 및 종목 뉴스/공시/기술·수급 해석: 실제 ai_generated=true인 경우 작은 AI 배지. 생성 상태와 모순되면 표시하지 않는다.
- 규칙 대체·자료 부족·실패 응답에는 AI 배지 없음. 캐시 재사용은 실제 생성 여부를 그대로 따른다.
- 신규 심층 텍스트 리포트는 생성 출처 플래그를 저장하고 본문에 배지 표시. 과거 플래그 없는 리포트는 임의로 AI 생성으로 추정하지 않는다. 인포그래픽 자체 및 간단 리서치의 출처 표시는 후속 점검 대상.
- 시즌 카드의 결론/근거/주의점/다음 확인 및 가설 배열 형식·길이·NaN/Inf를 검증. 잘못된 생성은 정상 캐시에 저장하지 않는다. 과거 캐시도 검사한다.
- 모델 payload가 AI 생성 여부·제공자·퀀트 점수 반영 여부 등 서버 출처 메타데이터를 덮어쓰지 못하게 변경.
- 원본 수치·종목 순위·기존 상세 정보는 삭제하거나 AI로 재계산하지 않았다.

## 검증 및 한계

Python 계약/시즌 카드/비용 경계/메뉴 근거 24개 통과. JS 배지/대체 응답/중복 표시/시즌 카드 3개 통과.
이는 자동 테스트 결과이지 실제 AI의 사실 정확성 평가가 아니다. 구조 검증은 허위 수치나 근거 없는 인과 설명을 완전히 탐지하지 못한다.
브라우저 시각 검증과 공개 배포는 이번 범위에서 하지 않았다.

## 남은 3단계 품질 승인 조건

공급자 정상화 후 무료 경로로 정상·약세·소표본·현재 근거 누락·오래된 기준일 5개 입력을 평가한다.
각 입력/출력/소요 시간/실패를 보존하고 다음을 직접 대조한다:

1. 종목·기준일·수익률 단위·부호가 원본과 일치하는가?
2. 과거 관측을 올해의 예측·실제 체결 수익으로 말하지 않는가?
3. 누락 수급/뉴스를 만들어내거나 0·호재로 간주하지 않는가?
4. 반대 근거와 구체적인 다음 확인을 짧게 제시하는가?
5. 공급자 실패 시 원본 UI가 유지되고 유료로 자동 전환하지 않는가?

성공 출력 검토 전 다른 메뉴로 분석 기능을 확대하지 않는다. 자동 재시도/유료 호출은 실행하지 않았다.
