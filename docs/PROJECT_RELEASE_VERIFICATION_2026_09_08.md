# KR Quant Research 릴리스 검증 — 2026-09-08

## 판정 원칙

프로젝트의 기능 완결과 투자 성과 검증은 별개다. 실제로 통과한 검사만 완료로 기록한다. 과거 자료가 없다는 사실을 면책 문구로 대체해 전체 모델 검증 완료로 처리하지 않는다.

## 이번에 수행한 작업

- 저장점 `savepoint-before-project-release-20260908` → `f247132`. 코드만 복구하며 DB·원천·배포본을 되돌리지 않는다.
- 토스 현재 원천 확인 후 160종목 재수집, 수집 오류 0. 9월7일 기준 현재 적격141, 제외19(상태18·기간1). 이전 캐시는 `data/research_snapshots/release_backups/20260908T005236947500-investor_flow.json`에 보존했다. KIS는 저장102종목 중 현재46종목으로, 전체 최신화라고 표시하지 않는다.
- 수급 툴팁이 스크롤 직후 닫히는 오류 재현·수정. 스크롤 후에도 포인터 아래에 있는 셀만 다시 표시하고 이탈 시 닫는다. PC E2E 19개 통과.
- DART 오류 테스트는 가짜 키로 완전 모킹한다. 종목 수급 API 테스트는 실제 계정/캐시에 의존하지 않는다. 피크 캐시 테스트는 독립 모듈을 사용해 다른 테스트의 백그라운드 계산을 오염시키지 않는다.
- 신규 수급 재수집 도구는 실패 시 원문 응답·인증정보를 출력하지 않고, 사전검사 후 본 수집이 실패해도 성공 상태를 반환하지 않는다. 관련 수집 테스트7개, 수급/캐시 격리 회귀25개 통과(중복 가능하므로 합산 총계 아님).
- 실제 데이터 통합11개 통과: 후보282개 상태·가격·거래량, 대시보드/캘린더 TOP3·카드·상세 일치, 중복 신호 없음, 오래된 세대 거절, 메뉴 간 현재 수급 일치. TOP3 168360·304360·111770.
- 공개 빌드: API29, 스크리너13, 상세2765, 리포트11. 파일3633. 금지 파일·키 패턴·로컬 경로 검사 통과. 품질 게이트 ready=true, 9월7일 적격729. 이 검사는 보안 위험 0%를 의미하지 않는다.
- 공개 정적판을 별도 로컬 ASGI 서버에서 열어13메뉴 전환·화면 표시 확인. JS 오류0, 로컬 HTTP 실패0. 차트/모달 모든 조합을 검사했다는 의미는 아니다.

## 검증 증거

- `output/audit/release-flow-refresh-20260908.json`
- `output/audit/release-e2e-fixed-20260908.xml`
- `output/audit/release-refresh-tests-20260908.xml`
- `output/audit/release-isolation-fixes-20260908.xml`
- `output/item2-integration-audit.json`
- `output/audit/release-public-browser-v2-20260908.json` 및 PNG
- `output/release-public-20260908/build.json`
- 전체 단위검사는 `output/release-isolated-20260908` 복사본에서 실행. `.env` 미복사, 자격증명 환경변수 제거, 네트워크 차단(Windows asyncio 내부 socketpair만 허용), 데이터·DB 복사본 사용. 원본 사용자 자료를 테스트 대상으로 변경하지 않는다.
- 첫 격리 결과는 검사 환경 문제를 포함해 실패했다. 데이터 복사 후608/610 통과, 실패2개는 위 테스트 격리 보완 후 전체 재실행한다. 최종 결과는 아래 추가 기록을 우선한다.

## 완료 처리할 수 없는 핵심

공식 기업행위·상폐 정산 원본이 없고, 거래상태6기준일·과거 점수14기준일·유니버스1기준일뿐이다. 현재 입력을 과거 날짜로 복제해서 당시 데이터라고 만들 수 없다. 기존 PIT 감사 `output/audit/pit-final-20260908.json`은 NOT_READY다.

따라서 전체 운영 선정기의 과거 입력 재생, 완전한 생존편향 통제, 미열람 OOS/워크포워드, 다중검정 및 실제 비용·체결을 반영한 성과 인증은 미완료다. 현재 시즌 상승률·피크 추정은 연구용 통계이지 검증된 수익 보장이 아니다.

필요한 후속 입력: 제공 권한을 갖춘 공식 기업행위/상폐정산 이력, 당시 모집단·거래상태 및 최초/정정 공시 버전. 이 자료의 수급 범위·비용·권한이 결정되어야 완전한 역사적 검증을 진행할 수 있다. 이 보고서는 해당 과제를 완료로 바꾸는 승인서가 아니다.

## 보존

사용자가 변경한 `data/calendar_momentum_portfolio.json`과 감사 실패 PNG2개는 유지하며 릴리스 커밋에서 제외한다. 공개 배포는 별도 성공 확인 기록이 있기 전까지 완료로 간주하지 않는다.
