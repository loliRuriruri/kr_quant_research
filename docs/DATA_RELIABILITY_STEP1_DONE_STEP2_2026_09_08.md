# 데이터 신뢰성 1단계 완료 범위와 2단계 진행

기준: 2026-09-08 장전, 최신 기대 종가 2026-09-07. 작업 전 코드 b05d7bd.

## 1단계: 운영 최신성 보완 완료

- 실제 시세·점수·전략 소스 기준일9/7 일치. 기존 시세9/7·점수9/4 불일치 해소.
- `refresh-local` 관리 작업 추가: 로컬 재계산과 공개 배포 분리. 작업 계산이 끝나도 후처리/배포 스레드가 살아 있으면 실행중으로 표시하고 중복 작업 차단.
- OpenDART 최근 정기공시 목록을 페이지 끝까지 대조. 초기14일, 성공 체크포인트와 하루 겹쳐 공백 추적. 89일 초과 공백은 자동 성공으로 처리하지 않음.
- 실제 누락 정정: 지에프씨생명과학388610 반기2026.06, receipt `20260907000314`. 기존 성공 수집 캐시가 신규 정정을 막던 경로에 표적 강제갱신 적용. 정정 후 재계산 완료 전까지 pending을 유지해 재시도 시 같은 날짜 점수로 건너뛰지 않음.
- 원천 변경 전 백업: `data/research_snapshots/financial_revisions/20260908T082441348962.parquet` (6,674,597바이트). 정정 receipt 저장 후 재계산 완료 확인.
- 오류 응답/빈 성공 금융응답은 대조 실패. 매핑 불가 공시는 partial. 최근공시 대조는 모든 기업·모든 과거 공시·비12월 결산의 완전한 회계 검증을 의미하지 않음. DART2540/2543은 행 보유율일 뿐 완비율이 아님.
- KIS 종목 집합 변경 시 당일 성공 기록만으로 건너뛰지 않음. 재계산 후 신규13대상 지연을 발견해 수집. 현재대상33/33 기대일 확인, 수집2889행, 오류0. 누적96종목 전체 최신이라는 표현 제거.
- 장전9/8 행이 응답에 함께 포함되므로 max날짜만으로9/7 정상 수집을 실패 판정하지 않음. 당일 미확정 행은 보관하되 확정으로 표시하지 않음.

## 2단계: 후보·화면 일관성 검증 및 미확정 신호 차단

- 공식 수급 `is_final=True`만 신호·차트·표본 리밸런싱 산출에 사용. False/표시누락 대규모 매수 행이 기존 확정 신호를 바꾸지 못하는 회귀 테스트 추가.
- 실제 시즌 선취매282후보 모두9/7 상태 ACTIVE, 양수 종가·거래량 통과. 거래정지/자료미확인 종목이 이 집합에 있는 사례 없음. 이는 공식 상태/당일 거래 자료에 대한 검사이며 모든 장중 제약의 증명은 아님.
- 대시보드 TOP3와 캘린더 TOP3, 카드 날짜·가격·진입단계, 신호ID 유일성, 상세 TOP10, 이전 세대 거절 검증. 총7항목 모두 통과.
- 재실행: `.venv\Scripts\python.exe scripts/audit_item2_integration.py`; 근거 `output/item2-integration-audit.json` (08:48:46, 282후보, 부적격 목록 빈 배열).
- 남은2단계: 오래된 보관 수급에 대한 현재 후보 날짜 게이트, 토스/스마트수급/랭킹/관심 상세까지 기준 통합. 2단계 전체 완료로 보고하지 않음.

## 검증·운영·복구

- 최종 관련 회귀 테스트154개 통과(40.82초). `node --check` 및 `git diff --check` 통과. 전체 저장소 테스트를 모두 실행한 결과는 아님.
- 수집은 실제 운영 관리 작업으로 수행. 테스트는 모의 원천을 사용하며 API 키를 출력하지 않음. 미확정/빈응답/변경유니버스/정정반영·백업·재계산 재시도/작업 후처리 경합 회귀검사 추가.
- 기존 사용자 자동설정으로 스마트 작업 뒤 공개 갱신도 성공: `https://7fdb16fb.korea-quant-research.pages.dev` (08:37경). 이후 최종 수급·신호 패치는 로컬에 적용, 추가 공개 배포는 수행하지 않음. GitHub push 없음.
- 서버8790 재시작 완료. 원천 계약 기준 최신성과 재계산 성공을 확인했으며 투자 수익률이나 전체 종목 선정의 정확성을 보장하지 않음.
- 코드 복구점 `savepoint-before-reliability12-20260908` → b05d7bd. 원천/DB/공개배포는 git 복구로 돌아가지 않음. 금융 facts 복구 시 서버를 정지하고 위 백업을 별도 검토해야 하며, 현재 DB를 무조건 덮어쓰지 말 것.
- 기존 사용자 변경 `data/calendar_momentum_portfolio.json` 및 감사실패 PNG 두 개는 커밋에서 제외.
