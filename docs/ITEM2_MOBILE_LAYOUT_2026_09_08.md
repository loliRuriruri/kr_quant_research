# 2번 후속: 작은 화면 보기 / PC 보기 보존

작업 전: a06d7d4. 엔진·선정 규칙·저장 데이터 변경 없음.

## 반영
- 기본 viewport를 화면 폭에 맞춤. 상단에서 PC 넓게 보기(1200px, 확대 허용) 선택 가능. 브라우저별 선택 저장, 저장소 접근 실패시에도 사용 가능.
- 좁은 화면 전체 메뉴 접기/펼치기, 메뉴 이동·Escape시 닫기, aria-expanded 반영.
- 헤더 줄바꿈, 카드 단일 열, 표 내부 가로 스크롤, 상세 패널 화면 폭 제한. 420px 이하 KPI 단일 열로 글자 잘림 완화.
- JS/CSS 2.81.0. 서버 재시작 없이 로컬 새로고침 적용.

## 확인
- Chromium 390/700/1280px × 대시보드·랭킹·수급·시즌·실행 파이프라인 15경로 ready. 문서 가로 넘침 없음, 메뉴 이동 후 닫힘. JS pageerror 0.
- PC 선택 후 reload 유지, 자동 보기 복원, Escape 닫기 확인.
- 실제 화면 이미지 확인 후 390px KPI 두 열의 글자 잘림을 단일 열로 보정.
- 기존 관련 테스트 42 pass + 새 레이아웃 계약 검사 1 pass, Node 문법/diff 검사 통과.
- 재현 스크립트: scripts/verify_mobile_layout.js. 최초 760px 경계 검사에서 버튼 표시 여부 가정으로 timeout: 실제 표시 확인 방식으로 보정, 700px 재검증.
- 이미지: output/playwright/mobile-layout-final.png. 출력 파일은 커밋 제외.

## 한계 / 다음
- 실제 iOS Safari/Android 터치·핀치, 모든 상세 팝업/특수 차트까지 검증한 것은 아님. 복잡한 내용은 패널 내 스크롤이 남을 수 있음.
- 공개 12개월 자료 누락, 원천 최신 일봉 수집 및 품질 통과 후 배포 확인은 별도 잔여 작업. 2번 전체 완료 아님.
- 기존 자동 스케줄은 변경하지 않음. UI 검사에서 수집/재계산/포트폴리오 쓰기 버튼을 누르지 않음. 기존 GET 연동 동작은 유지.
- 사용자 portfolio 변경·감사 PNG 보존. 공개 배포/GitHub push 없음.
- 롤백: savepoint-before-mobile-layout-20260908 태그(a06d7d4). 변경 취소는 이 UI 커밋을 git revert하는 방식 권장. 사용자 데이터를 포함하는 강제 reset 금지.
